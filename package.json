{
  "name": "web-ntd-building",
  "version": "1.0.0",
  "private": true,
  "description": "NTD Carpentry & Building - one-page site served as Cloudflare Workers static assets",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "social": "node social/render.js"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "wrangler": "^4.0.0"
  }
}
